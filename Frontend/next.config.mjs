/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      // For modern features support
      serverActions: true,
    },
  }
  
  export default nextConfig